# Development keys

These deterministic Ed25519 seeds exist only to make the local four-process demo reproducible. They are public test material, not production credentials. Replace them with independently generated secrets before any non-local deployment.
